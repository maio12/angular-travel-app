import { HotelCardImagePipe } from './hotel-card-image.pipe';

describe('HotelCardImagePipe', () => {
  it('create an instance', () => {
    const pipe = new HotelCardImagePipe();
    expect(pipe).toBeTruthy();
  });
});
